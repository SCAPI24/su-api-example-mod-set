# 2026-09-23 19:00 → 09-24 04:10 会话问题清单（2.2.0）

> 来源：本仓库工作区会话记录（DSH）在 2026-09-23 19:00 之后提出的全部问题/指令，按时间顺序去重整理。
> **19:00–21:10 期间没有任何用户发言**，实际从 21:10:59 开始。
> 深度根因与技术细节见同目录 `PROJECT-LOG.md` 的「2026-09-24 联机死亡/复活/血量 + 加入房间卡死」一节；
> 本文件是**完整问题清单**（含工具/流程类），便于自查与回归。

## A. 联机玩法与同步

| 时间(09-23) | 问题 | 处置 / 状态 |
|---|---|---|
| 21:10 | 睡觉睡醒之后饱食度没减 | 用户要求**先不检测**，仅记录（见 `PROJECT-LOG.md` 候选因 1/2）→ **未修** |
| 21:15–21:18 | 挖仙人掌：Windows 端已挖掉，平板端看不到、无掉落物；客户端长出了服务端没有的仙人掌 | 定位为客户端也在跑植物生长 → 改为**客户端不跑植物生长** ✓ |
| 22:46–22:54 | 挖 1 个仙人掌连带顶部掉落 2 个，只捡到 1 个（新挖掉 3 捡 3 正常） | 定位到背包"入包/出包"增量被合并/覆盖（`TryBuildInventoryDelta`/`ApplyInventoryDelta` 的基准是共享状态） |
| 22:57–23:44 | 平板边扔边捡连续丢件：14→13、13→10、11→7、丢光不回收、扔出去后快捷栏仍在 | 全量快照抖动 + 基准被 `MarkHostInventoryAuthoritative` 重置；用户要求回到增量模式 |
| 23:39 | 创造模式全量快照时网络跳变很大，物品拿不到快捷栏 | 同上 |
| 23:48–23:51 | 丢弃物品后"权威数据又回到本地"：地上有掉落物、快捷栏也还在 | 同上 |

## B. 工具与流程（本次会话踩到的自身问题）

| 时间(09-23) | 问题 | 处置 / 规则 |
|---|---|---|
| 21:30–21:37 | 命令超时没生效：单条命令跑了 318 秒才结束（远端重启世界其实几秒到十几秒就完成） | 用户定规则：**长任务走后台、保留较大上限、普通命令依赖较短默认**；世界加载要按无头端 `world load` 的实际耗时判断 |
| 21:39 | 每次命令写错就要等很久 | 同上；先分析再发，不盲发 |
| 21:50–21:56 | 世界加载只需几秒~十几秒，却被 hard wait 数分钟 | 用户指出应走无头 mod 的 load world 流程 |
| 23:50 | — | **硬规则**：此后所有命令 `timeout_ms` **只允许 30000**；执行失败要重新分析后另发，**不允许失败后立即连续重发** |

## C. 2.2.x 回归与服务端事故

| 时间(09-23) | 问题 | 处置 / 状态 |
|---|---|---|
| 22:04 | 服务端**加载世界完全卡死** | 2.2.1 引入的改动所致（后来注释掉该改动）；配合混淆保留名单问题一并排查 |
| 22:07 | 客户端无法进入选择地图界面，直接返回主菜单 | 同上；`SuPlayScreen.Enter` 抛异常被屏幕管理器兜回 |
| 22:17 | **开着代理就闪退到桌面**：`SuPlayScreen.Enter` NullReference（关闭代理即正常） | 当时以整体回退规避；**根因未定位，待复现** |
| 22:29 | 替换类 / `Pak/Database.xml` 读入时校验不通过 | 收敛为"按类名字符串引用的类型必须进 `Obfuscar.xml` 的 `<SkipType>`"（见 `DEV-RULES.md` 专题） |
| 22:40–22:42 | 存档 `Project.xml` 不得出现被替换成 mod 的类型名（原版只映射原版类型） | 已核查 `World`/`World1`/`World2` 与服务器存档 **0 命中** ✓ |
| 22:13 | — | 用户要求**整段回退 2.1.66** 并把问题记入 doc ✓ |

## D. 死亡 / 血量链路（09-24 00:36 → 03:16）

| 时间(09-24) | 问题 | 处置 / 状态 |
|---|---|---|
| 00:36–01:14 | 客户端角色死亡后**原地站着**；复活界面里还在提示饥饿扣血；另一端起看这个角色仍是活的 | 用户拍定**方案 B**：客户端原生伤害只作表现、血量不允许到 0、死亡一律以主机广播为准 ✓ |
| 00:48–01:05 | 排查过程中我连续走错：误点复活、把主机角色当成客户端角色、用 index 判身份 | 用户纠正：**用命令行桥查本端**、**不要乱写脚本**、**倒查角色名字而不是 index** |
| 01:06 / 01:09 | 我提出的"给化身补倒地因子"被否决；又被要求**不允许加日志** | 改用既有诊断 + 命令行桥取证 |
| 01:15 | — | 用户明确方案 B 的边界：现有"伤害上报"保留（客户端只上报扣血，主机扣到 0 才死） |
| 01:20–02:24 | 点击扣血一连串：死不掉 / 只闪红不掉血 / 慢点不死快点才死 / 2 格半就死 / 界面关了人不死 / 只剩 1 格时流感和中毒不死 | 收敛为"血量改为**主机通知、本地只跟随**"；流感/中毒非致命是引擎硬编码地板（`ComponentFlu`/`ComponentSickness`），按用户要求仅说明 ✓ |
| 02:44–03:05 | 死亡原因显示 **Unknown**、统计里没有这次死亡 | 见 `PROJECT-LOG.md` §1：击退通道死亡快照不带死因 + 一次性落地锁死 ✓ |
| 02:57 | 用户指出"主机端应该能拿到死因，主机端是实时模拟的" | **方向纠正**：撤回"客户端自己拼死因再上报"的实现（会误判） ✓ |
| 03:04 | 复活后立刻又出现死亡界面且无法操作；后续被狮子咬死的统计没有 | 见 `PROJECT-LOG.md` §2c：复活窗口 + 死亡落地标记复位 ✓ |
| 03:12 | 复活位置与主机不一致，其他客户端看到角色**闪现过去** | 见 `PROJECT-LOG.md` §3：主机回传落点 ✓ |
| 03:16 | 最后半格点击扣血不死 / 要等一会再点才死 | 见 `PROJECT-LOG.md` §2b、§5 ✓ |
| 03:22 | 复活后**屏幕全红 + 二次死亡界面 + 站着不能动** | 见 `PROJECT-LOG.md` §2a ✓ |
| 03:27 | 被其他动物攻击时只看得到扣血，**屏幕不闪红**（单机有） | 见 `PROJECT-LOG.md` §4 ✓ |
| 03:31 | 平板**卡在加入房间**界面，且要求"尽可能不要直接关闭" | 只读取证：电路 Recovery 保持态死等地形移动靶（见 §6）；现场日志已先保全 ✓ |
| 03:37 | "检查 CKT 和 Fence，fence 为啥一直在变延迟" | 见 `PROJECT-LOG.md` §7：锯齿 + 不可靠通道丢包 ✓ |
| 03:43 | 用户指定三条修法：**fence 700ms 就请求同步 / 加入阶段触发地形补推 / 诊断补三个数** | 全部实现并验证 ✓ |
| 03:45 | 半格（不死）与刚好一格（能死）的差异 | 采样定案：余量 0.005 < 上报阈值 0.02 被当小额攒账（§5） ✓ |

## E. 仍未闭环

1. **睡觉醒来饱食度不减**：用户要求先不检测，未动代码（`PROJECT-LOG.md` 有两个候选因）。
2. **背包同步"单调版本号"**：方案已选定、实现清单已写，**未实现**（`PROJECT-LOG.md` 末节）。
3. **开代理闪退**（`SuPlayScreen.Enter` NRE）：当时靠回退规避，根因未定位。
4. **加入房间的模态框没有超时收尾**；主机放弃/中止某客户端加入时**不通知该客户端**（"半个加入"）。
5. 主机 `SetCondition` 的 `Member 'm_pukeParticleSystem' is not of type Game.PukeParticleSystem`（GM 数据修改路径，未修）。
6. **Obfuscar 漏项自检**未做（`SuComponentSickness` 曾经漏掉，导致服务端 `Loading error`）。
7. 主机自己没算到的那部分伤害，客户端死亡原因仍可能是 Unknown。
8. 平板经代理的 `latest` 通道丢包只是被"700ms 主动请求同步"缓解，未根治。

## F. 本次会话沉淀的通用规则（已并入 `DEV-RULES.md`）

- 实测优于推断：能用命令行桥/既有诊断量化的，不要靠读代码猜；
- 身份识别一律用**角色名字/坐标**，禁用 index（两端 index 语义相反）；
- 不允许加日志时，用既有诊断日志 + 命令行桥取样；
- 心跳/延迟类指标要先分清"锯齿"与"故障"（`fenceAge` 是"距上条被接受 fence"的锯齿）；
- 三端 build 锁定：任何改动必须三端同包部署后再验证；
- 引擎把 `CauseOfDeath`/死亡统计只写在 `Injure()` 里 —— 凡是绕过 `Injure` 直接写血量的路径都要自己补这两样。
